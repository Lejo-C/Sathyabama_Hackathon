# PRAHARI backend dependencies (free-tier only, no paid services)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Level 2/3/4 verification stack
ddgs>=9.0                 # free DuckDuckGo web search, no API key
requests>=2.31            # HTTP fetching of sites / career pages
beautifulsoup4>=4.12      # HTML parsing
python-whois>=0.9         # domain age / registrant lookup
dnspython>=2.6            # MX record validation
tldextract>=5.1           # clean domain parsing
rapidfuzz>=3.6            # fuzzy name/domain matching, typosquat detection

# Tests
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27              # async client + FastAPI TestClient transport
